{
  "name": "texas-holdem",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "local": "bash dev-local.sh",
    "test:evaluator": "node server/src/handEvaluator.test.js",
    "test:settlement": "node server/src/gameEngine.settlement.test.js",
    "test:room": "node server/src/roomManager.test.js",
    "test": "npm run test:evaluator && npm run test:settlement && npm run test:room",
    "postinstall": "npm --prefix server install"
  },
  "engines": {
    "node": ">=18"
  }
}
